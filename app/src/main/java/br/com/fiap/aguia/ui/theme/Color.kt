package br.com.fiap.aguia.ui.theme

import androidx.compose.ui.graphics.Color

// Cores Principais - Identidade Aguia Branca
val PrimaryBlue = Color(0xFF0055A5)      // Azul principal da Aguia Branca
val SecondaryBlue = Color(0xFF0073CF)    // Azul secundario mais claro
val PrimaryYellow = Color(0xFFFFCC00)    // Amarelo marcante da Aguia Branca
val DarkBlue = Color(0xFF003380)         // Azul escuro para textos e logo

// Fundos
val BackgroundLight = Color(0xFFF8F9FA)  // Fundo geral claro
val SurfaceWhite = Color(0xFFFFFFFF)     // Cards e superficies

// Cores de Texto
val TextPrimary = Color(0xFF1A1A2E)      // Texto principal escuro
val TextSecondary = Color(0xFF6C757D)    // Texto secundario

// Cores Semanticas
val SuccessGreen = Color(0xFF28A745)     // Aprovado / positivo
val WarningYellow = Color(0xFFFFCC00)    // Em Analise (usa o amarelo da marca)
val ErrorRed = Color(0xFFDC3545)         // Rejeitado / erro